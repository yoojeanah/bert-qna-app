services:
  - type: web
    name: qa-study-backend
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    envVars:
      - key: PORT
        value: 5000
    plan: free
    rootDir: backend
