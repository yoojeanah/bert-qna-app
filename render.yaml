services:
  - type: web
    name: qa-study-app
    env: docker
    plan: free
    region: oregon
    dockerfilePath: ./Dockerfile
    buildCommand: "" # 🔑 Dockerfile 사용 시 buildCommand 비워둬도 됨
    startCommand: "" # 🔑 gunicorn 명령은 Dockerfile에 있음
    envVars:
      - key: PORT
        value: 5000
